import { useCallback, useEffect, useState } from 'react'
import { Link, useParams } from 'react-router-dom'
import { ArrowLeft, ScanLine } from 'lucide-react'
import { supabase } from '../../lib/supabase'

type Point = { id: string; name: string }; type Element = { id: string; label: string; element_type: string; metadata: { object_type?: string; access_point_id?: string } | null }
export default function OperacionPlanoAdmin() { const { eventId } = useParams(); const [points, setPoints] = useState<Point[]>([]), [elements, setElements] = useState<Element[]>([]), [error, setError] = useState<string | null>(null); const load = useCallback(async () => { if (!eventId) return; const { data: map } = await supabase.from('venue_maps').select('id').eq('event_id', eventId).maybeSingle(); const [pointResult, elementResult] = await Promise.all([supabase.from('access_points').select('id,name').eq('event_id', eventId).order('name'), map ? supabase.from('venue_map_elements').select('id,label,element_type,metadata').eq('map_id', map.id).in('element_type', ['access_point', 'zone']) : Promise.resolve({ data: [], error: null })]); setPoints((pointResult.data ?? []) as Point[]); setElements(((elementResult.data ?? []) as Element[]).filter((item) => item.element_type === 'access_point' || item.metadata?.object_type === 'door' || item.metadata?.object_type === 'access' || item.metadata?.object_type === 'security')); if (pointResult.error || elementResult.error) setError(pointResult.error?.message ?? elementResult.error?.message ?? 'No se pudo cargar.') }, [eventId]); useEffect(() => { void load() }, [load]); async function link(element: Element, accessPointId: string) { const { error: updateError } = await supabase.from('venue_map_elements').update({ metadata: { ...element.metadata, access_point_id: accessPointId || null } }).eq('id', element.id); if (updateError) setError(updateError.message); else await load() } const unresolved = elements.filter((item) => !item.metadata?.access_point_id).length; return <div className="min-h-[100dvh] bg-zinc-50"><header className="border-b bg-white"><div className="mx-auto flex max-w-4xl items-center gap-3 px-5 py-4"><Link to={`/admin/stands/${eventId}`}><ArrowLeft className="h-4 w-4" /></Link><ScanLine className="h-5 w-5 text-emerald-700" /><span className="font-semibold">Operación del plano</span></div></header><main className="mx-auto max-w-4xl px-5 py-8"><h1 className="text-2xl font-bold">Plan de control de accesos</h1><p className="mt-1 text-sm text-zinc-600">Cada puerta o acceso del plano necesita una decisión: dejarlo libre, o asignarle un punto de check-in para que un verificador escanee pases allí.</p><div className="mt-4 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-950"><strong>{unresolved} accesos sin verificador.</strong> Crea puntos en <Link to="/admin/puntos-acceso" className="underline">Puntos de acceso</Link> y asígnalos aquí.</div>{error && <p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-800">{error}</p>}<div className="mt-6 space-y-3">{elements.map((element) => <div key={element.id} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border bg-white p-4"><div><p className="font-semibold">{element.label}</p><p className="text-xs text-zinc-600">{element.metadata?.object_type === 'security' ? 'Garita / verificador' : 'Puerta o paso de circulación'}</p></div><select value={element.metadata?.access_point_id ?? ''} onChange={(event) => link(element, event.target.value)} className="rounded-lg border p-2 text-sm"><option value="">No requiere verificación</option>{points.map((point) => <option key={point.id} value={point.id}>{point.name}</option>)}</select></div>)}{!elements.length && <p className="rounded-xl border bg-white p-4 text-sm text-zinc-600">Añade puertas, accesos o garitas desde el plano para decidir dónde se controla el flujo.</p>}</div></main></div> }
