import { useCallback, useEffect, useState } from 'react'
import { Link, useParams } from 'react-router-dom'
import { ArrowLeft, Handshake, Plus } from 'lucide-react'
import { supabase } from '../../lib/supabase'

type EventRow = { id: string; name: string; organization_id: string }
type Company = { id: string; name: string }
type Package = { id: string; name: string; price: number | null; currency: string; description: string | null }
type Sponsor = { id: string; company_id: string; package_id: string | null; status: string; agreed_amount: number | null }
type Session = { id: string; name: string; session_type: string }
const field = 'rounded-lg border border-zinc-300 bg-white px-3 py-2 text-sm'

export default function PatrocinantesAdmin() {
  const { eventId } = useParams(); const [event, setEvent] = useState<EventRow | null>(null); const [companies, setCompanies] = useState<Company[]>([]); const [packages, setPackages] = useState<Package[]>([]); const [sponsors, setSponsors] = useState<Sponsor[]>([]); const [sessions, setSessions] = useState<Session[]>([]); const [companyId, setCompanyId] = useState(''); const [packageId, setPackageId] = useState(''); const [status, setStatus] = useState('prospect'); const [activation, setActivation] = useState(''); const [activationType, setActivationType] = useState('coffee_break'); const [sessionId, setSessionId] = useState(''); const [error, setError] = useState<string | null>(null); const [busy, setBusy] = useState(false)
  const load = useCallback(async () => { if (!eventId) return; const eventResult = await supabase.from('events').select('id,name,organization_id').eq('id', eventId).maybeSingle(); if (eventResult.error || !eventResult.data) { setError(eventResult.error?.message ?? 'Evento no encontrado.'); return }; const e = eventResult.data as EventRow; setEvent(e); const [companyResult, packageResult, sponsorResult, sessionResult] = await Promise.all([supabase.from('companies').select('id,name').eq('organization_id', e.organization_id).in('kind', ['sponsor', 'partner']).order('name'), supabase.from('sponsorship_packages').select('id,name,price,currency,description').eq('organization_id', e.organization_id).eq('is_active', true).order('name'), supabase.from('event_sponsorships').select('id,company_id,package_id,status,agreed_amount').eq('event_id', eventId).order('created_at'), supabase.from('event_sessions').select('id,name,session_type').eq('event_id', eventId).order('starts_at')]); setCompanies((companyResult.data ?? []) as Company[]); setPackages((packageResult.data ?? []) as Package[]); setSponsors((sponsorResult.data ?? []) as Sponsor[]); setSessions((sessionResult.data ?? []) as Session[]); setError(companyResult.error?.message ?? packageResult.error?.message ?? sponsorResult.error?.message ?? null) }, [eventId]); useEffect(() => { void load() }, [load])
  async function createCompany() { if (!event) return; const name = window.prompt('Nombre de la empresa patrocinante')?.trim(); if (!name) return; const contactName = window.prompt('Persona de contacto (opcional)')?.trim() || null; const result = await supabase.from('companies').insert({ organization_id: event.organization_id, name, kind: 'sponsor', contact_name: contactName }).select('id,name').single(); if (result.error || !result.data) { setError(result.error?.message ?? 'No se pudo crear la empresa patrocinante.'); return }; setCompanies((current) => [...current, result.data as Company].sort((a, b) => a.name.localeCompare(b.name))); setCompanyId(result.data.id); setError(null) }
  async function createPackage() { if (!event) return; const name = window.prompt('Nombre del paquete, por ejemplo Oro')?.trim(); if (!name) return; const result = await supabase.from('sponsorship_packages').insert({ organization_id: event.organization_id, name, description: 'Paquete comercial configurable' }); if (result.error) setError(result.error.message); else await load() }
  async function assign(form: React.FormEvent) { form.preventDefault(); if (!event || !companyId) return; setBusy(true); const result = await supabase.from('event_sponsorships').upsert({ organization_id: event.organization_id, event_id: event.id, company_id: companyId, package_id: packageId || null, status, agreed_amount: null }, { onConflict: 'event_id,company_id' }).select('id').single(); if (!result.error && result.data && activation.trim()) { const activationResult = await supabase.from('sponsorship_activations').insert({ event_sponsorship_id: result.data.id, activation_type: activationType, label: activation.trim(), session_id: sessionId || null }); if (activationResult.error) setError(activationResult.error.message) }; setBusy(false); if (result.error) setError(result.error.message); else { setActivation(''); await load() } }
  return <div className="min-h-[100dvh] bg-zinc-50"><header className="border-b bg-white"><div className="mx-auto flex max-w-6xl items-center gap-3 px-5 py-4"><Link to="/admin/eventos"><ArrowLeft className="h-4 w-4" /></Link><Handshake className="h-5 w-5 text-emerald-700" /><span className="font-semibold">Patrocinantes</span></div></header><main className="mx-auto max-w-6xl px-5 py-8"><h1 className="text-2xl font-bold">Plan comercial · {event?.name ?? 'Evento'}</h1><p className="mt-1 text-sm text-zinc-600">Asigna paquetes, controla entregables y vincula coffee breaks o lunch patrocinados.</p>{error && <p role="alert" className="mt-4 rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-800">{error}</p>}<section className="mt-6 grid gap-6 lg:grid-cols-2"><form onSubmit={assign} className="rounded-2xl border bg-white p-5"><div className="flex items-center justify-between gap-3"><h2 className="font-semibold">Asignar patrocinante y activación</h2><button type="button" onClick={() => void createCompany()} className="inline-flex items-center gap-1 rounded-lg border border-emerald-300 px-3 py-2 text-xs font-semibold text-emerald-700 hover:bg-emerald-50"><Plus className="h-3.5 w-3.5" />Crear empresa</button></div><p className="mt-1 text-xs text-zinc-500">Crea aquí la empresa si todavía no aparece en el selector.</p><div className="mt-4 grid gap-3"><select className={field} value={companyId} onChange={(e) => setCompanyId(e.target.value)}><option value="">Empresa patrocinante</option>{companies.map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}</select><select className={field} value={packageId} onChange={(e) => setPackageId(e.target.value)}><option value="">Sin paquete</option>{packages.map((p) => <option key={p.id} value={p.id}>{p.name}{p.price != null ? ` · ${p.price} ${p.currency}` : ''}</option>)}</select><select className={field} value={status} onChange={(e) => setStatus(e.target.value)}>{['prospect', 'proposed', 'confirmed', 'active', 'fulfilled', 'cancelled'].map((value) => <option key={value} value={value}>{value}</option>)}</select><input className={field} value={activation} onChange={(e) => setActivation(e.target.value)} placeholder="Activación opcional: Coffee break patrocinado" /><div className="grid grid-cols-2 gap-3"><select className={field} value={activationType} onChange={(e) => setActivationType(e.target.value)}><option value="coffee_break">Coffee break</option><option value="lunch">Lunch</option><option value="welcome">Bienvenida</option><option value="closing">Cierre</option><option value="session">Sesión</option><option value="other">Otra</option></select><select className={field} value={sessionId} onChange={(e) => setSessionId(e.target.value)}><option value="">Sin sesión</option>{sessions.map((s) => <option key={s.id} value={s.id}>{s.name}</option>)}</select></div></div><button disabled={busy || !companyId} className="mt-4 inline-flex items-center gap-2 rounded-lg bg-emerald-700 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-50"><Plus className="h-4 w-4" />Guardar asignación</button></form><section className="rounded-2xl border bg-white p-5"><div className="flex items-center justify-between"><h2 className="font-semibold">Paquetes comerciales</h2><button type="button" onClick={() => void createPackage()} className="rounded-lg border px-3 py-2 text-xs font-semibold">Nuevo paquete</button></div><ul className="mt-4 space-y-2">{packages.map((p) => <li key={p.id} className="rounded-lg bg-zinc-50 p-3 text-sm"><strong>{p.name}</strong>{p.description && <span className="ml-2 text-zinc-600">{p.description}</span>}</li>)}{!packages.length && <li className="text-sm text-zinc-500">Crea el primer paquete Oro, Plata o a medida.</li>}</ul></section></section><section className="mt-6 rounded-2xl border bg-white p-5"><h2 className="font-semibold">Patrocinantes asignados</h2><div className="mt-4 space-y-2">{sponsors.map((s) => <div key={s.id} className="flex flex-wrap justify-between gap-2 rounded-xl bg-zinc-50 p-3 text-sm"><span><strong>{companies.find((c) => c.id === s.company_id)?.name ?? 'Empresa'}</strong><span className="ml-2 text-zinc-600">{packages.find((p) => p.id === s.package_id)?.name ?? 'Sin paquete'} · {s.status}</span></span><span className="text-zinc-500">{s.agreed_amount ?? 'Monto pendiente'}</span></div>)}{!sponsors.length && <p className="text-sm text-zinc-500">Aún no hay patrocinantes asignados.</p>}</div></section></main></div>
}
