import { useCallback, useEffect, useMemo, useState } from "react";
import { Armchair, DoorOpen, Eye, EyeOff, FileUp, Flower2, Info, Lock, LogIn, Maximize2, Move, Plus, ShieldCheck, Sofa, Table2, Trash2, Unlock } from "lucide-react";
import { useNavigate } from "react-router-dom";
import { supabase } from "../../lib/supabase";
import { ExhibitionKonvaStage } from "./exhibition/ExhibitionKonvaStage";

type Kind = "stand" | "aisle" | "door" | "access" | "security" | "column" | "plant" | "table" | "sofa" | "information" | "flow_arrow" | "special";
type Geometry = { x: number; y: number; width: number; height: number; rotation?: number };
type SceneElement = { id: string; label: string; element_type?: string; status?: string; geometry: Geometry; layer: string; z_index: number; locked: boolean; visible: boolean; style: Record<string, unknown>; metadata: Record<string, unknown> };
type Company = { id: string; name: string };
type Assignment = { element_id: string; company_id: string };

const WORLD = { width: 40, height: 24 };
const DEFAULTS: Record<Kind, { label: string; width: number; height: number; layer: string; color: string }> = {
  stand: { label: "Stand", width: 3, height: 3, layer: "layout", color: "#d1fae5" }, aisle: { label: "Pasillo", width: 4, height: 1.5, layer: "circulation", color: "#cbd5e1" }, door: { label: "Puerta", width: 1.2, height: 0.35, layer: "circulation", color: "#fef3c7" }, access: { label: "Acceso", width: 1.5, height: 1, layer: "circulation", color: "#dbeafe" }, security: { label: "Verificador", width: 2, height: 1.5, layer: "circulation", color: "#fee2e2" }, column: { label: "Columna", width: 0.6, height: 0.6, layer: "architecture", color: "#94a3b8" }, plant: { label: "Planta", width: 1, height: 1, layer: "layout", color: "#dcfce7" }, table: { label: "Mesa", width: 2, height: 1, layer: "layout", color: "#fef3c7" }, sofa: { label: "Sofá", width: 2.5, height: 1, layer: "layout", color: "#ede9fe" }, information: { label: "Información", width: 2.5, height: 1, layer: "layout", color: "#e0f2fe" }, flow_arrow: { label: "Flujo", width: 3, height: 0.25, layer: "annotation", color: "#2563eb" }, special: { label: "Área especial", width: 4, height: 3, layer: "layout", color: "#f3e8ff" },
};
const palette: { kind: Kind; label: string; icon: typeof Armchair }[] = [
  { kind: "stand", label: "Stand", icon: Armchair }, { kind: "aisle", label: "Pasillo", icon: Move }, { kind: "door", label: "Puerta", icon: DoorOpen }, { kind: "access", label: "Acceso", icon: LogIn }, { kind: "security", label: "Verificador", icon: ShieldCheck }, { kind: "column", label: "Columna", icon: Plus }, { kind: "plant", label: "Planta", icon: Flower2 }, { kind: "table", label: "Mesa", icon: Table2 }, { kind: "sofa", label: "Sofá", icon: Sofa }, { kind: "information", label: "Información", icon: Info }, { kind: "flow_arrow", label: "Flujo", icon: Move }, { kind: "special", label: "Área especial", icon: Maximize2 },
];

const kindOf = (item: SceneElement): Kind => (item.metadata.object_type as Kind | undefined) ?? (item.element_type === "stand" ? "stand" : item.element_type === "aisle" ? "aisle" : item.element_type === "stage" ? "special" : "information");
const dbType = (kind: Kind) => kind === "stand" ? "stand" : kind === "aisle" ? "aisle" : kind === "special" ? "stage" : kind === "door" || kind === "access" || kind === "security" ? "access_point" : "zone";
function parseGeometry(item: Record<string, unknown>): Geometry { const raw = item.geometry; if (raw && typeof raw === "object") return raw as Geometry; const metadata = item.metadata && typeof item.metadata === "object" ? item.metadata as Record<string, unknown> : {}; return { x: Number(item.x ?? 0), y: Number(item.y ?? 0), width: Number(item.width ?? 1), height: Number(item.height ?? 1), rotation: Number(metadata.rotation ?? 0) }; }

export function ExhibitionCanvasEditor({ mapId, eventId }: { mapId: string; eventId: string }) {
  const navigate = useNavigate();
  const [elements, setElements] = useState<SceneElement[]>([]); const [metadata, setMetadata] = useState<Record<string, unknown>>({}); const [selectedId, setSelectedId] = useState<string | null>(null); const [selectedIds, setSelectedIds] = useState<string[]>([]); const [tool, setTool] = useState<Kind | null>(null); const [companies, setCompanies] = useState<Company[]>([]); const [assignments, setAssignments] = useState<Assignment[]>([]); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); const [backgroundUrl, setBackgroundUrl] = useState<string | null>(null); const [backgroundPath, setBackgroundPath] = useState<string | null>(null); const [opacity, setOpacity] = useState(65); const [showGrid, setShowGrid] = useState(true); const [snap, setSnap] = useState(true);
  const selected = elements.find((item) => item.id === selectedId) ?? null;
  const companyNames = useMemo(() => new Map(companies.map((company) => [company.id, company.name])), [companies]);
  const assignmentMap = useMemo(() => new Map(assignments.map((item) => [item.element_id, companyNames.get(item.company_id) ?? item.company_id])), [assignments, companyNames]);
  const columns = Number(metadata.width_units ?? WORLD.width); const rows = Number(metadata.height_units ?? WORLD.height);

  const load = useCallback(async () => {
    const [{ data: map, error: mapError }, { data: event, error: eventError }] = await Promise.all([supabase.from("venue_maps").select("metadata").eq("id", mapId).single(), supabase.from("events").select("organization_id").eq("id", eventId).single()]);
    if (mapError || eventError || !map) { setMessage(mapError?.message ?? eventError?.message ?? "No se pudo cargar el plano."); return; }
    const { data, error } = await supabase.from("venue_map_elements").select("id,label,element_type,status,x,y,width,height,metadata,geometry,layer,z_index,locked,visible,style").eq("map_id", mapId).order("z_index");
    if (error) { setMessage(error.message); return; }
    const nextMetadata = { ...(map.metadata ?? {}), plan_type: "exhibition_canvas", coordinate_system: "metric", width_units: Number(map.metadata?.width_units ?? WORLD.width), height_units: Number(map.metadata?.height_units ?? WORLD.height), snap_step: Number(map.metadata?.snap_step ?? 1) };
    setMetadata(nextMetadata);
    if (JSON.stringify(map.metadata ?? {}) !== JSON.stringify(nextMetadata)) await supabase.from("venue_maps").update({ metadata: nextMetadata }).eq("id", mapId);
    const normalized = (data ?? []).map((item) => { const raw = item as unknown as Record<string, unknown>; const itemMetadata = raw.metadata && typeof raw.metadata === "object" ? raw.metadata as Record<string, unknown> : {}; const elementType = String(raw.element_type ?? "zone"); return { ...raw, geometry: parseGeometry(raw), layer: String(raw.layer ?? itemMetadata.layer ?? (elementType === "stand" ? "layout" : "architecture")), z_index: Number(raw.z_index ?? itemMetadata.z_index ?? 1), locked: Boolean(raw.locked ?? itemMetadata.locked), visible: raw.visible !== false && itemMetadata.visible !== false, style: raw.style && typeof raw.style === "object" ? raw.style as Record<string, unknown> : { fill: itemMetadata.color ?? "#e2e8f0" }, metadata: itemMetadata } as unknown as SceneElement; });
    setElements(normalized); setSelectedId((current) => normalized.some((item) => item.id === current) ? current : null); setSelectedIds((current) => current.filter((id) => normalized.some((item) => item.id === id)));
    if (event?.organization_id) { const [firms, booth] = await Promise.all([supabase.from("companies").select("id,name").eq("organization_id", event.organization_id).eq("kind", "exhibitor").order("name"), supabase.from("booth_assignments").select("element_id,company_id").neq("status", "cancelled")]); setCompanies((firms.data ?? []) as Company[]); setAssignments((booth.data ?? []) as Assignment[]); }
    const path = nextMetadata.background_path as string | undefined; setBackgroundPath(path ?? null); if (path) { const signed = await supabase.storage.from("agenda-attachments").createSignedUrl(path, 3600); if (!signed.error) setBackgroundUrl(signed.data.signedUrl); }
  }, [eventId, mapId]);
  useEffect(() => { void load(); }, [load]);

  async function persist(item: SceneElement) { const g = item.geometry; const nextMetadata = { ...item.metadata, object_type: kindOf(item) }; const { error } = await supabase.from("venue_map_elements").update({ x: g.x, y: g.y, width: g.width, height: g.height, geometry: g, layer: item.layer, z_index: item.z_index, locked: item.locked, visible: item.visible, style: item.style, metadata: nextMetadata }).eq("id", item.id); if (error) setMessage(error.message); }
  function selectElement(item: SceneElement, additive: boolean) { setSelectedIds((current) => additive ? current.includes(item.id) ? current.filter((id) => id !== item.id) : [...current, item.id] : [item.id]); setSelectedId(item.id); }
  function moveSelection(sourceId: string, x: number, y: number) { const source = elements.find((item) => item.id === sourceId); if (!source) return; const ids = selectedIds.includes(sourceId) ? selectedIds : [sourceId]; const dx = x - source.geometry.x; const dy = y - source.geometry.y; const nextItems = elements.filter((item) => ids.includes(item.id) && !item.locked).map((item) => ({ ...item, geometry: { ...item.geometry, x: Math.max(0, Math.min(columns - item.geometry.width, item.geometry.x + dx)), y: Math.max(0, Math.min(rows - item.geometry.height, item.geometry.y + dy)) } })); setElements((current) => current.map((item) => nextItems.find((next) => next.id === item.id) ?? item)); nextItems.forEach((item) => void persist(item)); }
  async function addAt(kind: Kind, x: number, y: number) { const preset = DEFAULTS[kind]; const count = elements.filter((item) => kindOf(item) === kind).length + 1; const geometry = { x: Math.max(0, Math.min(x, columns - preset.width)), y: Math.max(0, Math.min(y, rows - preset.height)), width: preset.width, height: preset.height, rotation: 0 }; const payload = { map_id: mapId, element_type: dbType(kind), label: `${preset.label} ${count}`, x: geometry.x, y: geometry.y, width: geometry.width, height: geometry.height, status: kind === "stand" ? "available" : "blocked", geometry, layer: preset.layer, z_index: kind === "stand" ? 20 : 10, locked: false, visible: true, style: { fill: preset.color }, metadata: { floorplan_kind: kind === "stand" ? "stand" : "object", object_type: kind, purpose: preset.label } }; setBusy(true); const { data, error } = await supabase.from("venue_map_elements").insert(payload).select("id").single(); setBusy(false); if (error || !data) { setMessage(error?.message ?? "No se pudo añadir el elemento."); return; } setTool(null); setSelectedId(data.id); setSelectedIds([data.id]); setMessage(`${preset.label} añadido.`); await load(); }
  async function removeSelected() { const ids = selectedIds.length ? selectedIds : selected ? [selected.id] : []; const deletable = elements.filter((item) => ids.includes(item.id) && !item.locked); if (!deletable.length || !window.confirm(`¿Eliminar ${deletable.length} elemento(s) seleccionado(s)?`)) return; const { error } = await supabase.from("venue_map_elements").delete().in("id", deletable.map((item) => item.id)); if (error) setMessage(error.message); else { setSelectedId(null); setSelectedIds([]); await load(); } }
  async function removePlan() { if (!window.confirm("¿Eliminar el plano completo, sus stands, objetos y asignaciones? El evento se conservará.")) return; const { error } = await supabase.from("venue_maps").delete().eq("id", mapId); if (error) setMessage(error.message); else navigate("/admin/eventos"); }
  async function updateSelected(change: Partial<SceneElement>) { if (!selected) return; const next = { ...selected, ...change, geometry: { ...selected.geometry, ...(change.geometry ?? {}) } }; setElements((current) => current.map((item) => item.id === next.id ? next : item)); await persist(next); }
  async function assign(companyId: string) { if (!selected || kindOf(selected) !== "stand" || !companyId) return; const { error } = await supabase.from("booth_assignments").upsert({ element_id: selected.id, company_id: companyId, status: "confirmed" }, { onConflict: "element_id" }); if (error) setMessage(error.message); else { await supabase.from("venue_map_elements").update({ status: "assigned" }).eq("id", selected.id); setAssignments((items) => [...items.filter((item) => item.element_id !== selected.id), { element_id: selected.id, company_id: companyId }]); setMessage("Empresa asignada."); await load(); } }
  async function release() { if (!selected) return; const { error } = await supabase.from("booth_assignments").delete().eq("element_id", selected.id); if (error) setMessage(error.message); else { await supabase.from("venue_map_elements").update({ status: "available" }).eq("id", selected.id); setAssignments((items) => items.filter((item) => item.element_id !== selected.id)); setMessage("Stand liberado."); await load(); } }
  async function uploadBackground(file: File) { const ext = file.name.split(".").pop()?.toLowerCase() ?? "bin"; const path = `${String(metadata.organization_id ?? "org")}/${eventId}/map-${mapId}/background-${Date.now()}.${ext}`; setBusy(true); const { error } = await supabase.storage.from("agenda-attachments").upload(path, file, { upsert: true, contentType: file.type }); if (!error) { const next = { ...metadata, background_path: path, background_name: file.name, background_mime: file.type }; await supabase.from("venue_maps").update({ metadata: next }).eq("id", mapId); setMetadata(next); setBackgroundPath(path); const signed = await supabase.storage.from("agenda-attachments").createSignedUrl(path, 3600); setBackgroundUrl(signed.data?.signedUrl ?? null); setMessage("Plano base cargado."); } else setMessage(error.message); setBusy(false); }

  return <section className="grid gap-4 xl:grid-cols-[220px_1fr_280px]"><aside className="rounded-2xl border bg-white p-4"><h2 className="font-semibold">Biblioteca</h2><p className="mt-1 text-xs text-zinc-500">Selecciona un objeto y haz clic en el plano para colocarlo.</p><div className="mt-4 grid grid-cols-2 gap-2">{palette.map(({ kind, label, icon: Icon }) => <button key={kind} type="button" onClick={() => setTool(tool === kind ? null : kind)} className={`flex items-center gap-1 rounded-lg border p-2 text-left text-xs font-semibold ${tool === kind ? "border-emerald-600 bg-emerald-50" : "bg-white"}`}><Icon className="h-4 w-4" />{label}</button>)}</div><label className="mt-5 flex cursor-pointer items-center gap-2 rounded-lg border border-dashed p-3 text-xs font-semibold"><FileUp className="h-4 w-4" />Cargar plano base<input type="file" accept=".pdf,.png,.jpg,.jpeg,.svg" className="hidden" disabled={busy} onChange={(event) => { const file = event.target.files?.[0]; if (file) void uploadBackground(file); }} /></label><p className="mt-2 text-[11px] text-zinc-500">PDF, PNG, JPG o SVG. DXF se incorporará después.</p><div className="mt-4 space-y-2 text-xs"><label className="flex items-center justify-between">Mostrar rejilla<input type="checkbox" checked={showGrid} onChange={(event) => setShowGrid(event.target.checked)} /></label><label className="flex items-center justify-between">Snap<input type="checkbox" checked={snap} onChange={(event) => setSnap(event.target.checked)} /></label><label className="flex items-center justify-between">Opacidad<input type="range" min="15" max="100" value={opacity} onChange={(event) => setOpacity(Number(event.target.value))} /></label></div></aside><main><div className="mb-3 flex flex-wrap items-center gap-2 rounded-xl border bg-white p-3 text-xs"><span className="font-semibold">Canvas a escala: {columns} × {rows} m</span><span className="text-zinc-500">{elements.length} elementos</span>{selectedIds.length > 0 && <button type="button" onClick={() => void removeSelected()} className="rounded border border-red-200 px-2 py-1 font-semibold text-red-700">Eliminar {selectedIds.length}</button>}{tool && <span className="rounded bg-emerald-50 px-2 py-1 text-emerald-800">Colocando: {DEFAULTS[tool].label}</span>}<button type="button" onClick={() => void removePlan()} className="ml-auto rounded border border-red-200 px-2 py-1 font-semibold text-red-700">Eliminar plano</button><button type="button" onClick={() => setShowGrid((value) => !value)} className="rounded border p-1.5" title="Mostrar u ocultar rejilla">{showGrid ? <Eye className="h-4 w-4" /> : <EyeOff className="h-4 w-4" />}</button></div><div className="overflow-auto rounded-2xl border bg-slate-100 p-3"><ExhibitionKonvaStage columns={columns} rows={rows} elements={elements} assignments={assignmentMap} selectedIds={selectedIds} showGrid={showGrid} snap={snap} backgroundUrl={backgroundUrl} opacity={opacity} tool={tool} onSelect={selectElement} onClear={() => { setSelectedId(null); setSelectedIds([]); setTool(null); }} onPlace={(x, y) => { if (tool) void addAt(tool, x, y); }} onMove={moveSelection} onTransform={(id, geometry) => { const item = elements.find((candidate) => candidate.id === id); if (item) void updateSelected({ ...item, geometry }); }} /></div>{message && <p className="mt-2 rounded-lg bg-amber-50 p-2 text-xs text-amber-900">{message}</p>}<p className="mt-2 text-xs text-zinc-500">Arrastra, redimensiona y rota. Ctrl/Cmd + clic permite seleccionar varios elementos.</p></main><aside className="h-fit rounded-2xl border bg-white p-4">{selected ? <Inspector item={selected} companies={companies} assignment={assignments.find((item) => item.element_id === selected.id)?.company_id ?? ""} onChange={updateSelected} onAssign={assign} onRelease={release} onDelete={removeSelected} /> : <div className="text-sm text-zinc-600"><h2 className="font-semibold text-zinc-900">Inspector</h2><p className="mt-2">Selecciona un elemento para editar posición, tamaño, rotación, capa o empresa.</p>{backgroundPath && <p className="mt-4 rounded-lg bg-emerald-50 p-3 text-xs text-emerald-900">Plano base: {String(metadata.background_name ?? "")}</p>}</div>}</aside></section>;
}

function Inspector({ item, companies, assignment, onChange, onAssign, onRelease, onDelete }: { item: SceneElement; companies: Company[]; assignment: string; onChange: (change: Partial<SceneElement>) => void; onAssign: (id: string) => void; onRelease: () => void; onDelete: () => void }) { const kind = kindOf(item); return <div><div className="flex items-start justify-between gap-2"><div><h2 className="font-semibold">{item.label}</h2><p className="text-xs text-zinc-500">{DEFAULTS[kind]?.label ?? kind}</p></div><button type="button" onClick={onDelete} className="rounded border border-red-200 p-1 text-red-700" title="Eliminar"><Trash2 className="h-4 w-4" /></button></div><div className="mt-4 grid grid-cols-2 gap-2 text-xs"><label>X<input type="number" step="0.1" value={item.geometry.x} onChange={(event) => onChange({ geometry: { ...item.geometry, x: Number(event.target.value) } })} className="mt-1 w-full rounded border p-2" /></label><label>Y<input type="number" step="0.1" value={item.geometry.y} onChange={(event) => onChange({ geometry: { ...item.geometry, y: Number(event.target.value) } })} className="mt-1 w-full rounded border p-2" /></label><label>Ancho<input type="number" step="0.1" min="0.1" value={item.geometry.width} onChange={(event) => onChange({ geometry: { ...item.geometry, width: Number(event.target.value) } })} className="mt-1 w-full rounded border p-2" /></label><label>Alto<input type="number" step="0.1" min="0.1" value={item.geometry.height} onChange={(event) => onChange({ geometry: { ...item.geometry, height: Number(event.target.value) } })} className="mt-1 w-full rounded border p-2" /></label></div><label className="mt-3 block text-xs">Rotación<input type="number" step="15" value={item.geometry.rotation ?? 0} onChange={(event) => onChange({ geometry: { ...item.geometry, rotation: Number(event.target.value) } })} className="mt-1 w-full rounded border p-2" /></label><label className="mt-3 block text-xs">Etiqueta<input value={item.label} onChange={(event) => onChange({ label: event.target.value })} className="mt-1 w-full rounded border p-2" /></label><label className="mt-3 flex items-center gap-2 text-xs"><input type="checkbox" checked={item.locked} onChange={(event) => onChange({ locked: event.target.checked })} />{item.locked ? <Lock className="h-3.5 w-3.5" /> : <Unlock className="h-3.5 w-3.5" />} Bloquear elemento</label>{kind === "stand" && <><label className="mt-3 block text-xs">Empresa<select value={assignment} onChange={(event) => void onAssign(event.target.value)} className="mt-1 w-full rounded border p-2"><option value="">Seleccionar empresa</option>{companies.map((company) => <option key={company.id} value={company.id}>{company.name}</option>)}</select></label>{assignment && <button type="button" onClick={onRelease} className="mt-2 inline-flex items-center gap-1 rounded border px-2 py-1 text-xs"><Unlock className="h-3 w-3" />Liberar empresa</button>}</>}</div>; }
